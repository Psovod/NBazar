import { RealityLocationPipe } from './reality-location.pipe';

describe('RealityLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new RealityLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
