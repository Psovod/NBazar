import { EnergyColorCodePipe } from './energy-color-code.pipe';

describe('EnergyColorCodePipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyColorCodePipe();
    expect(pipe).toBeTruthy();
  });
});
