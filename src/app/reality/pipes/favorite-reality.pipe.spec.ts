import { FavoriteRealityPipe } from './favorite-reality.pipe';

describe('FavoriteRealityPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteRealityPipe();
    expect(pipe).toBeTruthy();
  });
});
