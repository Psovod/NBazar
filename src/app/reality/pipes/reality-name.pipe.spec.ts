import { RealityNamePipe } from './reality-name.pipe';

describe('RealityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RealityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
